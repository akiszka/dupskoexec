.gitignore

CC = clang
AR = ar
LD = ld

CFLAGS += -O2
CFLAGS += -Wall
CFLAGS += -Wextra

!cc = |> $(CC) %f -g -O0 -o %o $(CFLAGS) $(CPPFLAGS) |>
!ar = |> $(AR) rcs %o %f |>
!ld = |> $(LD) %f -o %o |>

: *.c |> !cc |> dupsko