.gitignore

CC = clang
AR = ar
LD = ld

CFLAGS += -O2
CFLAGS += -Wall
CFLAGS += -Wextra

!cc = |> $(CC) %f -o %o $(CFLAGS) $(CPPFLAGS) |>
!ar = |> $(AR) rcs %o %f |>
!ld = |> $(LD) %f -o %o |>

r: *.c |> !cc |> dupsko